services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"  # 81 local => 80 in container
    volumes:
      - './nginx/nginx.conf:/etc/nginx/nginx.conf'
      - './static_files:/usr/share/nginx/html/static_files'
      - './media:/usr/share/nginx/html/media'